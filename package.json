{
  "name": "aesthetix-studio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Velora — fine jewellery static site (htmx + Alpine.js, no build step)",
  "scripts": {
    "build": "node scripts/generate.js",
    "generate": "node scripts/generate.js",
    "serve": "node scripts/serve.mjs",
    "check": "node scripts/serve.mjs --check",
    "dev": "node scripts/generate.js && node scripts/serve.mjs"
  }
}
