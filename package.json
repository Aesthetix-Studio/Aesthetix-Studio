{
  "name": "aesthetix-studio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Aesthetix Studio — digital product studio site. Static HTML generated from templates, served by a zero-dependency Node server with a JSON API.",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "node scripts/generate.js",
    "generate": "node scripts/generate.js",
    "serve": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "check": "node scripts/serve.mjs --check",
    "dev": "node scripts/generate.js && node scripts/serve.mjs"
  }
}
